#!/bin/sh -e

export PREFIX=""
if [ -d 'venv' ] ; then
    export PREFIX="venv/bin/"
fi
export SOURCE_FILES="httpcore tests"

set -x

${PREFIX}black --check --diff --exclude httpcore/_sync --exclude tests/sync_tests --target-version=py36 $SOURCE_FILES
${PREFIX}mypy httpcore
scripts/unasync --check
